requires:
- category: "development"
  name: "mocaccino-overlay-x"
  version: ">=0"
unpack: true
env:
- FEATURES="-sandbox -usersandbox"
steps:
- USE=-harfbuzz emerge media-libs/freetype
- emerge dev-perl/XML-Parser
- emerge -uDNv --with-bdeps=y --backtrack=100 --autounmask-keep-masks=y @world
- perl-cleaner --all
